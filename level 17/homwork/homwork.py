# 1) ფუნქცია რომელიც გამოიტანს "გამარჯობა"
def say_hello():
    print("გამარჯობა")


# 2) ფუნქცია, რომელსაც გადააწოდებთ სახელს და გამოიტანს "გამარჯობა {სახელი}"
def say_hello_name(name):
    print(f"გამარჯობა {name}!")


# 3) ფუნქცია, რომელსაც გადააწოდებთ 2 რიცხვს და გამოიტანს მათ ჯამს
def sum_two_numbers(a, b):
    print(a + b)


# 4) ფუნქცია, რომელსაც გადააწოდებთ 3 რიცხვს და გამოიტანს მათ ნამრავლს
def multiply_three_numbers(a, b, c):
    print(a * b * c)


# 5) ფუნქცია, რომელიც ამოწმებს ასაკს >18
def is_adult(age):
    if age > 18:
        print(True)
    else:
        print(False)


# 6) ახსნა: რა არის global და local scope
# global scope — ცვლადი, რომელიც ფუნქციის გარეთაა და ყველგან ხელმისაწვდომია
# local scope — ცვლადი, რომელიც ფუნქციის შიგნით იქმნება და მხოლოდ იქ მუშაობს

x = 10

def test_scope():
    y = 5 
    print("global:", x)
    print("local:", y)


# 7) ახსნა: რა არის def
# def — ის keyword-ია, რომლითაც იქმნება ფუნქცია Python-ში.


# 8) ახსნა: რა ქვია იმ ნაწილს, რომელიც ფუნქციის მერე მოდის?
# ფუნქციის შემდეგ ბრჭყალებში () არის პარამეტრები.
# ფუნქციის შიგნით {} არ გამოიყენება — უბრალოდ indented კოდი.


# 9) ახსნა: რა არის პარამეტრი?
# პარამეტრი — ეს არის ცვლადი, რომელსაც ფუნქციას გადასცემთ ფუნქციის გამოძახებისას.


# 10) კალკულატორის ფუნქცია
def calculator(a, b):
    print("ჯამი:", a + b)
    print("სხვაობა:", a - b)
    print("ნამრავლი:", a * b)
    print("განაყოფი:", a / b)


# ფუნქციის გამოძახებები:
say_hello()
say_hello_name("გიორგი")
sum_two_numbers(5, 7)
multiply_three_numbers(2, 3, 4)
is_adult(20)
test_scope()
calculator(10, 5)
